diff --git a/node_modules/sonner/dist/index.js b/node_modules/sonner/dist/index.js
index bd21e89..c0c04ca 100644
--- a/node_modules/sonner/dist/index.js
+++ b/node_modules/sonner/dist/index.js
@@ -466,9 +466,14 @@ const Toast = (props)=>{
     const toastClassname = toast.className || '';
     const toastDescriptionClassname = toast.descriptionClassName || '';
     // Height index is used to calculate the offset as it gets updated before the toast array, which means we can calculate the new layout faster.
-    const heightIndex = React__default.default.useMemo(()=>heights.findIndex((height)=>height.toastId === toast.id) || 0, [
+    // Fallback to `index` when not yet in `heights` — `findIndex(...) || 0` is wrong for -1 (truthy), causing negative offsets and overlap.
+    const heightIndex = React__default.default.useMemo(()=>{
+        const i = heights.findIndex((height)=>height.toastId === toast.id);
+        return i >= 0 ? i : index;
+    }, [
         heights,
-        toast.id
+        toast.id,
+        index
     ]);
     const closeButton = React__default.default.useMemo(()=>{
         var _toast_closeButton;
@@ -514,7 +519,7 @@ const Toast = (props)=>{
         // Trigger enter animation without using CSS animation
         setMounted(true);
     }, []);
-    React__default.default.useEffect(()=>{
+    React__default.default.useLayoutEffect(()=>{
         const toastNode = toastRef.current;
         if (toastNode) {
             const height = toastNode.getBoundingClientRect().height;
diff --git a/node_modules/sonner/dist/index.mjs b/node_modules/sonner/dist/index.mjs
index d0c7a5a..ed49ff9 100644
--- a/node_modules/sonner/dist/index.mjs
+++ b/node_modules/sonner/dist/index.mjs
@@ -459,9 +459,14 @@ const Toast = (props)=>{
     const toastClassname = toast.className || '';
     const toastDescriptionClassname = toast.descriptionClassName || '';
     // Height index is used to calculate the offset as it gets updated before the toast array, which means we can calculate the new layout faster.
-    const heightIndex = React.useMemo(()=>heights.findIndex((height)=>height.toastId === toast.id) || 0, [
+    // Fallback to `index` when not yet in `heights` — `findIndex(...) || 0` is wrong for -1 (truthy), causing negative offsets and overlap.
+    const heightIndex = React.useMemo(()=>{
+        const i = heights.findIndex((height)=>height.toastId === toast.id);
+        return i >= 0 ? i : index;
+    }, [
         heights,
-        toast.id
+        toast.id,
+        index
     ]);
     const closeButton = React.useMemo(()=>{
         var _toast_closeButton;
@@ -507,7 +512,7 @@ const Toast = (props)=>{
         // Trigger enter animation without using CSS animation
         setMounted(true);
     }, []);
-    React.useEffect(()=>{
+    React.useLayoutEffect(()=>{
         const toastNode = toastRef.current;
         if (toastNode) {
             const height = toastNode.getBoundingClientRect().height;
